# From the v1 repo to v0.3

Upstream: `github.com/ApplePiesFromScratch/propagation-logic`

Kept:

- `P / G → Q` as the constant
- three knobs (renamed `Γ` → `G`)
- JSON carriers as *presentations*
- “code over manifesto” as a rule we actually apply

Removed or demoted:

- “This is not analogy. It is structural identity.”
- paradoxes as thermodynamic debt of the universe
- zero-cost distinction as the founding sin of the West
  (the *accounting* point stays: work off-book is a leak)
- PDFs as canon
- authored `forced: true` without a cut
- `UpdatedModel/` stub

Added:

- exact-Q `Reading` / `rate(P,G)` / certificates
- registered μ and `ChannelPreservationError`
- unified ledger (STEP, GUARD, RECONFIG, MIGRATE)
- prescreen ADMIT/DEFER/REFUSE
- five operational guardrails
- `PROCESS.py` as the resume file
- insights index

v1 `core/pl_unified.py` and `pl_paradox_engine.py` are not shipped.
They mixed receipts with terrain. Rebuild a paradox probe from
`prescreen` + a named carrier if you need one; do not restore the engine
as ontology.
